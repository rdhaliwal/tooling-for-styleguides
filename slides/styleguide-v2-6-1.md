## Grunt!

Simple to configure
```js
grunt.initConfig({
    less: {
        default: {
            files: {
                "path/to/result.css": "path/to/source.less"
            }
        }
    }
});
```
Simple to use
```bash
grunt less
```
